<?php

namespace Tests\Feature;

use function PHPUnit\Framework\assertTrue;

test('List all transactions (by authenticated user)', function () {
    assertTrue(true);
});

test('List all transactions by type', function () {
    assertTrue(true);
});

test('List all transactions by amount range', function () {
    assertTrue(true);
});

test('List all transactions by date range', function () {
    assertTrue(true);
});
